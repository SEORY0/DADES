---
interface Props { readonly class?: string; readonly label?: string; }
const { class: className, label } = Astro.props;
---
<section class:list={['glass-panel', className]} aria-label={label}><slot /></section>
